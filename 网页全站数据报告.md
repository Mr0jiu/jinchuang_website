# 网页全站数据报告

> 版本：MVP 正式产物版  
> 检查日期：2026-08-27  
> 检查范围：`dashboard_ui/index.html`、`dashboard_ui/modern_server.py`、`web1/model_bridge.py` 及 `jinchuang_v4/code/outputs/mvp`

## 1. 全站数据结论

网页目前是“业务数据库 + MVP 模型产物 + 实时模型服务”混合架构，不是所有页面都读取同一份数据。技术报告页的核心模型数据已与 MVP 正式产物对齐，但首页业务统计、贷款详情、复核记录仍来自 SQLite 数据库。

MVP 正式结果如下：

| 指标 | 当前值 | 来源 |
|---|---:|---|
| 图片总量 | 16,270 | `run_summary.json` |
| 有效图片 | 16,270（100%） | `run_summary.json` |
| 材料类别 | 5 类，每类 3,254 张 | `run_summary.json` |
| 分类测试准确率 | 100.00% | `classification_metrics.json` |
| Stage1 配对级 Precision / Recall / F1 | 99.75% / 99.62% / 99.68% | `two_stage_summary.json` |
| Stage1 组级 Precision / Recall / F1 | 98.23% / 95.24% / 96.71% | `two_stage_summary.json` |
| 唯一无向图片对 | 25,001 | `two_stage_summary.json` |
| 可疑配对 | 11,447（45.79%） | `fraud_monitoring_summary.json` |
| 跨客户欺诈 | 10,320（可疑配对的 90.15%） | `fraud_monitoring_summary.json` |
| 紧急复核 | 3,768 | `fraud_monitoring_summary.json` |
| 风险聚类 | 247 | `fraud_monitoring_summary.json` |
| 处理耗时 | 17.8 秒 | `run_summary.json` |

## 2. 页面模块数据来源

| 页面模块 | 主要接口 | 当前数据源 | 可靠性说明 |
|---|---|---|---|
| 首页总览 | `/api/stats` | SQLite 的客户、贷款、状态、特征和缓存统计 | 业务运营数据真实，但不完全等于 MVP 汇总口径 |
| 可疑交易 | `/api/suspicious`、`/api/loans/all` | SQLite 贷款表、相似组和复核状态 | 适合查看业务明细；需要数据库与模型产物同步 |
| 智能检测 | `/api/detect`、`/api/detect-group` | 当前模型桥、FAISS、影像目录 | 属于实时推理结果，不是固定 MVP 汇总 |
| 阈值策略 | `/api/policy`、`/api/policy/impact` | SQLite 策略配置 + 模型评估结果 | 页面阈值可修改，必须保留版本和操作人 |
| 同客户区分 | `/api/same-customer` | 实验 `results.json` 或归档目录 | 是实验结果，不应与 Stage1 指标混为一谈 |
| 操作记录 | `/api/operation-records`、`/api/operations` | SQLite 保存记录和复核日志 | 属于用户操作事实，不应由 MVP JSON 替代 |
| 技术报告 | `/api/report`、`/api/experiments` | `outputs/mvp` JSON/CSV + `evidence_bridge.py` | 核心汇总已对齐；部分明细文件当前缺失 |

## 3. 已修正的页面不一致

本次已更新 `dashboard_ui/index.html`：

- 页面初始旧指标 `99.87% / 99.87% / 99.64% / 99.76%` 改为真实的 `100.00% / 99.75% / 99.62% / 99.68%`。
- 指标名称改为“分类准确率、配对级精确率、配对级召回率、配对级 F1 分数”，明确不同评估口径。
- 将错误的“独立测试集 · 14,769 个影像对”改为“当前正式评估集 · 25,001 个唯一无向影像对”。
- 阈值实验区注明当前产物未提供 `threshold_experiment.csv` 明细。
- Stage1 样例区注明当前产物未提供 `stage1_similarity_report.csv` 明细，避免用户误以为暂无样例就是没有结果。
- 删除了用于延迟修正错误数字的页面逻辑；页面初始值和接口返回值现在一致。

## 4. 当前仍存在的数据缺口

当前 `outputs/mvp` 目录只包含汇总 JSON、模型文件、FAISS 索引和 `face_manifest.csv`，没有以下明细文件：

- `stage1_similarity_report.csv`
- `stage2_fraud_type_report.csv`
- `threshold_experiment.csv`
- `fraud_monitoring.csv`

因此网页可以可靠显示汇总指标，但技术报告页的阈值曲线、Stage1 具体样例和风险最高样例无法完全由当前目录恢复。后端已有回退逻辑：Stage2 类型分布可以从 `two_stage_summary.json` 读取；其他明细只能显示暂无数据，不能人为补齐。

## 5. 页面数据口径风险

1. 首页统计以 SQLite 当前贷款库为准，而技术报告以 `outputs/mvp` 正式产物为准；若数据库被新增、删除或复核更新，两处数字可能自然不同。
2. Stage1 配对级指标和组级指标不是同一测试集，网页必须保留“配对级/组级”标签。
3. 分类准确率 100% 只代表五类材料分类测试集，不代表欺诈检测准确率。
4. 11,447 个可疑配对是模型风险线索，不是已经人工确认的欺诈案件。
5. `final_report_metrics_after_review_labels` 的 100% 来自当前标签回溯口径，不能当作独立人工验证后的生产准确率。

## 6. 建议的最终数据架构

建议保留两类数据源，但明确职责：

- `outputs/mvp`：统一提供模型版本、评估指标、风险汇总、阈值和报告数据。
- SQLite：统一提供贷款详情、客户信息、影像路径、操作记录和人工复核状态。

网页所有指标应由后端接口计算或读取，前端只负责展示；每个接口返回 `source` 和 `updated_at`。这样既能保证模型报告真实可靠，也不会丢失业务复核过程。

## 7. 交付文件

- 页面更新：`dashboard_ui/index.html`
- 全站数据报告：`网页全站数据报告.md`
- MVP 数据报告：`jinchuang_v4/code/outputs/mvp/mvp_data_report.md`
